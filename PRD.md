# rag-lab — Product Requirements

Status: in development · Owner: @aboydnw · License: MIT

## Problem

RAG is easy to stand up and hard to trust. Tutorials and starter kits get you to a first answer, but they leave the real question unanswered: is this configuration actually any good, and did the change I just made help or hurt? There's no low-friction way to *measure* one RAG setup against another — to swap a chunker or a retriever, re-run a fixed question set, and watch the metric move.

Two things widen that measurement gap. Most kits assume a cloud LLM and an API key, so every experiment costs money and can't run in air-gapped or privacy-sensitive settings — a credit card and a vendor account sit between a developer and their first answer. And the newer agentic approaches are black boxes: the model orchestrates its own retrieval, but you can't see what it did or prove it beat a plain pipeline.

## What we're building

An open-source lab for building and *measuring* retrieval systems on models you run yourself. Point it at a markdown corpus (a directory or a GitHub repo), build a local index, and ask questions in plain language — answers come back cited, generated by a small local model via Ollama, no API keys required to run. But answering is table stakes; the point is the harness around it. Every component is swappable, every swap is scored against a golden set, and every eval report is committed so you can see how a change moved the numbers. Agentic retrieval is a first-class, glass-box citizen: you can watch a small model orchestrate its own retrieval and measure whether that actually beats a fixed pipeline.

**One-line pitch:** a RAG you can measure, not just a RAG that works — and it runs fully local.

## Who it's for

- **People learning RAG and agents** who want a small, readable, swappable system to experiment with — and a way to *prove* their changes helped instead of guessing.
- **Developers choosing a config** who need to know whether hybrid beats vector-only, or whether an agent beats a fixed pipeline, *on their own corpus* — not on someone else's benchmark.
- **Privacy- or cost-sensitive teams** who can't or won't send docs to a third-party API, and need the whole loop to run on hardware they control.

## Principles

1. **A RAG you can measure, not just a RAG that works.** Every component — chunker, retriever, embedder, LLM, agent — is swappable behind a clean interface, and every swap is justified by an eval delta against a baseline. The eval harness is the product's core differentiator, not the retrieval itself; the committed eval trail is the evidence.
2. **Fully local, zero secrets.** Clone, install, run — the only dependency is a local Ollama. No API keys to get a first answer. This isn't just a privacy stance: it's what makes cheap, repeatable experimentation possible. Optional cloud features (e.g. an LLM-judge for eval) are strictly opt-in and never required.

## Core capabilities

The product is three loops, each a CLI subcommand:

| Loop | Command | What it does |
|------|---------|--------------|
| **Ingest** | `rag-lab ingest <path-or-repo>` | Load docs → chunk → embed → write a local index. |
| **Query** | `rag-lab ask "question"` | Retrieve relevant chunks, generate an answer with a local LLM, print it with citations. |
| **Eval** | `rag-lab eval` | Run a golden Q&A set through the query loop and write a scored report. |

Supporting commands: `rag-lab inspect chunks` (debug what got indexed), `rag-lab config init` (write an editable config), and `rag-lab studio` (a local web cockpit over all three loops — see Current status).

## Requirements

### Must have (v1)
- Ingest a local markdown directory **and** a GitHub repo (`--repo owner/name`) into a single-file local index.
- Markdown-structure-aware chunking with a token cap and overlap.
- Local embeddings via Ollama.
- **Hybrid retrieval** (keyword/BM25 + vector, fused) — the default; vector-only and BM25-only also selectable.
- Answer generation via a local small model (default `llama3.2:3b`), with numbered citations mapping back to `filepath#heading`.
- A config file (`rag.yml`) that governs every swappable component.
- An **eval harness**: a golden set, deterministic retrieval scores (recall@k, MRR) and answer scores (keyword coverage), an optional LLM-judge, and a committed markdown report.
- A README that takes a new user from install to first answer in under five minutes (assuming Ollama is installed).

### Explicitly out of scope for v1
Multi-turn chat, cross-encoder reranking, query rewriting/HyDE, web crawler and PDF loaders, cloud LLM/embedding fallback, multi-corpus operation, and incremental re-indexing. Each is a natural follow-up the eval harness can later justify.

A local web UI (**studio**) and **agentic retrieval** were explicit post-v1 follow-ups and have since shipped — see below.

## Post-v1 direction: an agent lab

The project's purpose has widened beyond "RAG that works": it's a place to **build and study agents on local models** — turn the knobs, watch the reasoning, and measure the result. Agentic retrieval (shipped) is the first expression of that: the LLM orchestrates retrieval tools in a ReAct loop instead of running one fixed pipeline. The fixed retrievers stay: they're the measurable baseline every agent experiment is compared against. The boundary holds from the other side too — this is a learning lab, not an agent framework; features earn their place by making agent behavior more visible, tunable, or measurable.

## Success criteria

v1 ships when:

1. `rag-lab ingest --repo developmentseed/titiler` produces a working index from a fresh clone.
2. `rag-lab ask "..."` returns a coherent, cited answer using only local Ollama.
3. `rag-lab eval` runs a 20-question DevSeed golden set (titiler, eoAPI, stac-fastapi) and writes a report.
4. The README walks a new user from install to first answer in under five minutes.
5. The repo is public on GitHub under @aboydnw, MIT-licensed, with a committed baseline eval report.

## Current status

All three loops are built and tested: **ingest** (loader, markdown-aware chunker, Ollama embedder, sqlite-vec store), **query** (hybrid retrieval, cited answers via a local LLM), and **eval** (golden set, retrieval + answer scorers, optional LLM judge, committed markdown reports).

Beyond v1, **studio** (`rag-lab studio`) has shipped: a local Streamlit interface to turn every config knob, edit the golden set, run evals, and compare runs on a leaderboard. It reuses the same swappable components and adds a gitignored `.rag-lab/` workspace for cached indexes and run history. The studio is an optional extra; the core CLI runs without it.

**Agentic retrieval** has also shipped: an opt-in ReAct agent (`ask --agent`, `eval --agent`, or `agent.enabled` in `rag.yml`) that picks its own retrieval tools (vector search, keyword search, list documents, fetch document), with every knob exposed — step cap, tool subset, final context size, and the agent prompt itself. Eval scores agent runs with the same retrieval metrics as fixed-retriever runs (computed over the context that reached the answer), plus agent diagnostics (seen-vs-final recall gap, tool/LLM call counts), and persists per-question traces for inspection in the studio.

## Reference

Full architecture and component contracts live in the design spec: `~/Obsidian/Project Docs/rag-lab/specs/2026-05-23-rag-lab-design.md`. Implementation plans (ingest, query, eval) are in the sibling `plans/` folder.

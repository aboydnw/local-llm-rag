# rag-lab — Product Requirements

Status: in development · Owner: @aboydnw · License: MIT

## Problem

Most RAG tutorials and starter kits assume a cloud LLM and an API key. That puts a credit card and a vendor account between a developer and their first answer, and it makes the system impossible to run in air-gapped or privacy-sensitive settings. There's no low-friction way to point a tool at a docs corpus and ask it questions entirely on your own machine — and no easy way to *measure* whether one RAG configuration is actually better than another.

## What we're building

An open-source CLI that runs retrieval-augmented Q&A over any markdown docs corpus, fully local. Point it at a directory (or a GitHub repo) of docs, build a local index, and ask questions in plain language. Answers come back with citations, generated by a small local model via Ollama. No API keys required to run.

**One-line pitch:** RAG that doesn't need OpenAI.

## Who it's for

- **Developers evaluating a tool** who want to ask its docs questions instead of grepping them.
- **Privacy- or cost-sensitive teams** who can't or won't send docs to a third-party API.
- **People learning RAG** who want a small, readable, swappable system to experiment with — and a way to prove their changes helped.

## Principles

1. **Fully local, zero secrets.** Clone, install, run — the only dependency is a local Ollama. No API keys to get a first answer. Optional cloud features (e.g. an LLM-judge for eval) are strictly opt-in and never required.
2. **A RAG you can measure, not just a RAG that works.** Every component — chunker, retriever, embedder, LLM — is swappable behind a clean interface, and every swap is justified by an eval delta against a baseline. The eval harness is the product's core differentiator, not the retrieval itself.

## Core capabilities

The product is three loops, each a CLI subcommand:

| Loop | Command | What it does |
|------|---------|--------------|
| **Ingest** | `rag-lab ingest <path-or-repo>` | Load docs → chunk → embed → write a local index. |
| **Query** | `rag-lab ask "question"` | Retrieve relevant chunks, generate an answer with a local LLM, print it with citations. |
| **Eval** | `rag-lab eval` | Run a golden Q&A set through the query loop and write a scored report. |

Supporting commands: `rag-lab inspect chunks` (debug what got indexed) and `rag-lab config init` (write an editable config).

## Requirements

### Must have (v1)
- Ingest a local markdown directory **and** a GitHub repo (`--repo owner/name`) into a single-file local index.
- Markdown-structure-aware chunking with a token cap and overlap.
- Local embeddings via Ollama.
- **Hybrid retrieval** (keyword/BM25 + vector, fused) — the default; vector-only and BM25-only also selectable.
- Answer generation via a local small model (default `llama3.2:3b`), with numbered citations mapping back to `filepath#heading`.
- A config file (`rag.yml`) that governs every swappable component.
- An **eval harness**: a golden set, deterministic retrieval scores (recall@k, MRR) and answer scores (keyword coverage), an optional LLM-judge, and a committed markdown report.
- A README that takes a new user from install to first answer in under five minutes (assuming Ollama is installed).

### Explicitly out of scope for v1
Multi-turn chat, cross-encoder reranking, query rewriting/HyDE, web crawler and PDF loaders, cloud LLM/embedding fallback, web UI, multi-corpus operation, and incremental re-indexing. Each is a natural follow-up the eval harness can later justify.

## Success criteria

v1 ships when:

1. `rag-lab ingest --repo developmentseed/titiler` produces a working index from a fresh clone.
2. `rag-lab ask "..."` returns a coherent, cited answer using only local Ollama.
3. `rag-lab eval` runs a 20-question DevSeed golden set (titiler, eoAPI, stac-fastapi) and writes a report.
4. The README walks a new user from install to first answer in under five minutes.
5. The repo is public on GitHub under @aboydnw, MIT-licensed, with a committed baseline eval report.

## Current status

The **ingest loop** is built and tested (loader, markdown-aware chunker, Ollama embedder, sqlite-vec store, `ingest` command). The **query loop** and **eval loop** — the two pillars that carry the value prop — are not yet implemented.

## Reference

Full architecture and component contracts live in the design spec: `~/Obsidian/Project Docs/rag-lab/specs/2026-05-23-rag-lab-design.md`. Implementation plans (ingest, query, eval) are in the sibling `plans/` folder.
